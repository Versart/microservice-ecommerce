spring:
    application:
        name: registry
server:
    port: 8081
eureka:
    client:
        registerWithEureka: false
        fetch-registry: false
        serviceUrl:
            defaultZone: http://localhost:${server.port}/eureka/
